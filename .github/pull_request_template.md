## What changed

-

## Why

<!-- Link the issue if there is one: Fixes #123 -->

## How you tested it

<!-- The commands you ran. For anything visual in admin/ or mobile/, a before and after screenshot. -->

- [ ] `cd backend && go test ./...`
- [ ] `cd sandbox/agentd && python -m unittest discover -p "test_*.py"`
- [ ] `cd sdk/python && python -m unittest discover -s tests -p "test_*.py"`
- [ ] `cd admin && npm run build`
- [ ] `cd mobile && flutter analyze && flutter test`

<!-- Vulnerabilities go through SECURITY.md, not a public PR. -->
